# Ridge Evaluation Tests

- `test_evaluate.py`: builds a deterministic T90 test split and verifies Ridge predictions, aggregate quality, decade metrics, and saved evaluation artifacts.

The deterministic fixture checks evaluation behavior without depending on a full-dataset training run.
