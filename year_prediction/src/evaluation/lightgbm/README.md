# LightGBM Evaluation

This directory reserves the model-specific evaluation boundary for the LightGBM experiment.

- `evaluate.py`: will evaluate a saved LightGBM model on the fixed test benchmark.
- `validate.py`: will verify LightGBM model artifacts and saved validation results.

Commands and output contracts will be documented when these entry points are implemented.
