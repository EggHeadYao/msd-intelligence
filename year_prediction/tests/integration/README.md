# Integration Tests

- `test_ridge_pipeline.py`: runs a small Parquet-to-Ridge pipeline, checks all model artifacts, and rejects artist overlap between train and validation.

The synthetic integration dataset checks pipeline behavior, not final MSD prediction quality.
